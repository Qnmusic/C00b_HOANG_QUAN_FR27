khi clone về bạn hãy chạy lệnh npm install

và sau đó chạy lệnh tsc --watch
để biên dịch file typescript

tiếp đến là chạy lệnh sass --watch src/public/scss/main.scss src/public/css/main.css
để biên dịch scss sang file css